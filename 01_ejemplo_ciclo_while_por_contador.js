let contador = 1;

console.log("Hola");

while (contador <= 5) {
	//Instrucciones que se repiten mientras la condición sa verdadera
	console.log(`${contador}: Estoy aprendiendo ciclos`);
	contador = contador + 1;
	//esta última línea se puede escribir como
	//contador++; (esto es solo para contar de a uno)
	//contador += 1; (esto lo vi en w3 schools y es para que aumente en uno, en 7 o lo que sea).
}

 
console.log("Chau, gracias"); //Siempre se ejecuta