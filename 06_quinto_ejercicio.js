//Realizá un programa que permita al usuario ingresar valores del mismo tipo para las variables a y b. Una vez cargadas, la computadora debe mostrar ambas variables por pantalla, intercambiar entre sí sus valores (que lo cargado en a quede en b, y viceversa), y volver a mostrarlas.

let valorA = parseInt(prompt("Ingrese un valor para A"));
let valorB = parseInt(prompt("Ingrese un valor para B"));

console.log(`Se muestran A y B ${valorA} y ${valorB}`);
console.log(`Valores intercambiados Se muestran A y B ${valorB} y ${valorA}`);

